meta {
  name: Login User
  type: http
  seq: 2
}

post {
  url: {{Baseurl}}/api/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email":"jane.smith@example.com",
    "password":"password123"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
